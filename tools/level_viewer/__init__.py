"""
Developer viewer for an exported World-to-BeamNG level (terrain, roads, structures, water, forest, markers).

Start with ``python -m tools.level_viewer`` (see ``cli.py`` for the options). The data loaders
(``level_data``, ``geometry``, ``dae``) do not import pyvista and are unit-tested on their own.
"""
